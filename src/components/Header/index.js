import React from "react";

import "./styles.scss";

const Header = () => (
  <header id="mainHeader">
    <div className="container">
      <h1><a href="/">Trucker</a> </h1>
    </div>
  </header>
);

export default Header;
